{
  "name": "lawxygen",
  "version": "1.0.0",
  "private": true,
  "description": "LAWXYGEN Node.js + Express + EJS website",
  "main": "server.js",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wrangler dev",
    "dev:node": "nodemon server.js",
    "start": "npm run dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "wrangler": "^4.120.0"
  }
}
